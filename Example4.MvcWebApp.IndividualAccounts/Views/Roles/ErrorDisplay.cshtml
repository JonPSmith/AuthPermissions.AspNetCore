@model string

@{
    ViewData["Title"] = "ErrorDisplay";
}

@foreach (var error in @Model.Split(Environment.NewLine))
{
    <h3 class="text-danger">@error</h3>
}

<div>
    <a asp-action="Index">Back to home</a>
</div>
